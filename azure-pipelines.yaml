resources:
  repositories:
  - repository: airflow-github-sync
    type: github
    endpoint: ambika-garg
    name: airflow-github-sync

pr:
- main
- prod

trigger:
- main

pool: 
  vmImage: ubuntu-latest

jobs:
- job: CI
  - steps:
    - checkout: airflow-github-sync
